import Reducer from '../utils/LearnShuffling';

console.log(111);

/*
const DefaultStateTest = {
  errorMessage: 'わかりません もいちど',
  languageCode: 'ja-JP',
  currentFlashCard: null,
  shuffledFlashCards: [],


  flashCards: [
    { id: 1, preview: '日本語', translation: 'japanisch', romanji: 'nihongo' },
    { id: 2, preview: '見ました', translation: 'seen', romanji: 'mimashita' },
    { id: 3, preview: '日曜日', translation: 'sunday', romanji: 'nichiyobi' },
    { id: 4, preview: 'ドイツ', translation: 'germany', romanji: 'doitsu' },
  ],

  userStatistic: [],
};
*/
console.log(111);
describe('reducer tests', () => {
/*
  it('test default reducer', () => {
    const newState = Reducer();
    expect(JSON.stringify(newState)).toEqual(JSON.stringify(DefaultStateTest));
  });*/
/*
  it('test type: FLASH_CARD_FAULTY_LEARNED', () => {
    const newState = Reducer({
      type: 'FLASH_CARD_FAULTY_LEARNED',
      currentFlashCard: { id: 999 },
      userStatistic: [],
    });
    console.log(newState);
    expect(JSON.stringify(newState)).toEqual(JSON.stringify(DefaultState));
  }); */
});
